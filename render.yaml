services:
  - type: web
    name: heritagelens-ai
    runtime: python
    plan: free

    buildCommand: |
      pip install -r ai_app/requirements.txt

    startCommand: |
      streamlit run ai_app/app.py --server.port=$PORT --server.address=0.0.0.0
